sudo: required
services:
  - docker
language: haxe
haxe:
  - "3.2.1"
  - development
neko: "2.0.0"
matrix:
  allow_failures:
    - haxe: development
install:
  - sudo apt-get install -y nodejs
hxml:
  - ./tests/tests.hxml
script:
  - ./tests/run.sh